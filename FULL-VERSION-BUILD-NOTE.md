# FULL-VERSION-BUILD-NOTE

Purpose:
- Repackage the verified config-patched `v2.2.0` runtime as a full-version install package after test-deployment difficulty with build-candidate packaging.

What changed:
- Package-facing documentation was updated from build-candidate wording to full-version wording.
- Runtime application files remain aligned to `v2.2.0`.

What did not change:
- No new functional code changes were introduced by this repackaging step.
- Firebase config remains the verified inserted config from the prior config-patched build.
- Patch boundary remains inside the previously approved runtime files.
